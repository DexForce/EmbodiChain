### Atomic Action Class JSON Specs for $robot_display_name Dual-Arm Relative Placement

The deterministic graph uses only the native atomic action class JSON specs.
- `$first_arm` manipulates `$first_runtime_uid`.
- `$second_arm` manipulates `$second_runtime_uid`.

- First arm pick-up:
  $first_pick_spec
- Second arm pick-up:
  $second_pick_spec
$first_release_actions
$second_release_actions
- Keep a holding arm closed:
  $holding_spec
- Return to initial qpos:
  $return_spec
