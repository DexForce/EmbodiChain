Task:
$task_name: $task_prompt_summary

This config was generated from a simple task description by the config-stage
LLM. The task graph is precomputed from this deterministic specification.

Original simple task description:
$task_description

Config-stage LLM interpretation:
$action_sketch

Coordinated shared-object mapping:
- Use both arms together to pick and move `$moved_runtime_uid`.
- Source object: `$moved_source_uid`.
- Target relation: `$relation` ($relation_phrase) relative to
  `$reference_runtime_uid`.

$coordinate_convention

The deterministic nominal graph contains exactly 3 nominal edges. First it
uses `CoordinatedPickment` in `left_arm_action` with `right_arm_action` null.
Then it opens both grippers simultaneously and returns both empty arms to
their initial arm joint poses. It does not add separate `PickUp`,
`MoveHeldObject`, `Place`, or extra gripper/return actions.

1. Coordinated pick and move `$moved_runtime_uid`:
   - left_arm_action: $action_spec
   - right_arm_action: null

2. Release `$moved_runtime_uid` from both grippers:
   - left_arm_action: $left_release_spec
   - right_arm_action: $right_release_spec

3. Return both empty arms to their initial poses:
   - left_arm_action: $left_initial_spec
   - right_arm_action: $right_initial_spec

Final state: `$moved_runtime_uid` is $relation_phrase
`$reference_runtime_uid` and is not held by either gripper. Both arms are at
their initial arm joint poses with grippers open.
$final_planning_rule
