Task:
$task_name: $task_prompt_summary

This config was generated from an object-manipulation task description by the
config-stage LLM. The task graph is precomputed from this specification.

Original simple task description:
$task_description

Object and arm mapping:
- Hold-hover manipulation(s): $objects.
- Do not release any held object.
- Do not return a holding arm to its initial qpos.

The deterministic nominal graph contains exactly 3 nominal edges. It uses only
the atomic action class JSON specs below and adds no recovery, monitor, search,
release, placement, or return-to-initial edges. The final state keeps every
selected object hovering in a closed gripper.

$numbered_edges

Final state: every selected object remains lifted and held by its assigned
$robot_display_name arm in the exported $project_name environment config.
