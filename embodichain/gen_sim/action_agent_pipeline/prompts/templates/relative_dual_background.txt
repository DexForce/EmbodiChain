The scene comes from the exported $project_name mesh environment.

This configuration directory is for a $robot_display_name dual-arm
relative-placement task generated from a simple natural-language description.

$robot_context

Both arms participate in the nominal graph:
$placement_lines
$registry

Config-stage LLM notes:
$notes

$execution_rule
$placement_rule
