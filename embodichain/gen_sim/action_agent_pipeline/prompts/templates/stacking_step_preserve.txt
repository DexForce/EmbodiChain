$start_edge. Pick up `$runtime_uid` for stack layer $layer_index:
   - $active_slot: $pickup_spec
   - $inactive_slot: null

$edge_place. Place `$runtime_uid` directly at the final stack pose without changing orientation:
   - $active_slot: $place_spec
   - $inactive_slot: null

$edge_return. Return `$active_arm` to its initial pose:
   - $active_slot: $return_spec
   - $inactive_slot: null
